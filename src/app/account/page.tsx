import { AccountPageFlow } from '@/modules/Account/Module';

export default function AccountPage() {
  return (
    <div className='relative h-screen w-full'>
      <AccountPageFlow />
    </div>
  );
}
